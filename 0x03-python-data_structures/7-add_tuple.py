#!/usr/bin/python3

def add_tuple(tuple_a=(), tuple_b=()):
    sum_a = 0
    sum_b = 0

    if 
    sum_a = int(tuple_a[0]) + int(tuple_b[0])
    sum_b = int(tuple_a[1]) + int(tuple_b[1])
    return (sum_a, sum_b)

    """for a in range(0, len(tuple_a)):
        sum_a += tuple_a[a]

    for b in range(0, len(tuple_b)):
        sum_b += tuple_b[b]

    return (sum_a, sum_b)"""
